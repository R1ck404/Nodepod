# 022 — Process startup

Status: IN PROGRESS

Prefer the same-origin worker asset with a pre-init handshake and embedded fallback, minify publish output, and remove avoidable process initialization. Preserve automatic synchronous SQLite behavior; move it to a realm host only after the RPC implementation passes concurrency and no-SAB parity tests.
