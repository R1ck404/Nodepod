# 021 — Browser runtime benchmarks

Status: IN PROGRESS

Add a COOP/COEP Playwright harness for cold and persistent profiles, automatic runtime spans, `performanceStats()`, five-run result capture, and checked-in Chromium baselines. Keep microbenchmarks diagnostic only.

Done when Scelar boot, install, Vite-ready, preview, HMR, memory, and teardown gates run reproducibly in CI across the supported browser fallbacks.
